def print_42(a):
  d = {}
  for i in range(len(string)):
      if a[i] not in d:
          d[a[i]] = 0
      else
          d[a[i]] = d[a[i]] + 1
   cnt = 0
   for k in d:
       if d[k] != cnt:
           return -1
           break
       cnt = cnt+1

    return 42
